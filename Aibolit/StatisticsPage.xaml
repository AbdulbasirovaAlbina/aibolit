<Page x:Class="Aibolit.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Статистика ветеринаров">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок и кнопки -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Статистика ветеринаров" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          VerticalAlignment="Center"
                          Margin="0,0,20,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Период: с" 
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
                <DatePicker x:Name="StartDatePicker" 
                           Width="150"
                           SelectedDateChanged="StartDatePicker_SelectedDateChanged"
                           Margin="0,0,20,0"/>
                <TextBlock Text="по" 
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
                <DatePicker x:Name="EndDatePicker" 
                           Width="150"
                           SelectedDateChanged="EndDatePicker_SelectedDateChanged"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Таблица -->
        <DataGrid x:Name="StatisticsDataGrid" 
                 Grid.Row="1"
                 AutoGenerateColumns="True"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 GridLinesVisibility="All"
                 HeadersVisibility="Column"
                 AlternatingRowBackground="#F8F9FA"
                 AutoGeneratingColumn="StatisticsDataGrid_AutoGeneratingColumn"/>
    </Grid>
</Page>


